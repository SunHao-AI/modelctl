# 启动进度与日志可观测性

> 本文件为「模型启动进度可视化」计划（2026-09-08）沉淀问题的聚合入口；原始单文件已并入本文件归档以保留溯源信息。

## docker run --detach 路径下 launch log 只有容器 ID，SSE / CLI logs / 早退摘录全废

- **日期**：2026-09-08　**分类**：后端 / 日志

### 根因

vllm/tokenspeed/tensorrt_llm 走 docker runtime 时，启动命令是 `docker run --detach`：客户端进程秒返回，stdout 只打印一行**容器 ID**，容器的真实引擎输出（banner、权重加载、报错栈）全部留在 **daemon 侧**，`launch-<name>.log` 里根本没有。连锁失效面：

1. 详情页日志 SSE（`GET /admin/api/models/{name}/log/stream`）尾随的是 launch log —— 镜像拉完前整个日志窗口只有容器 ID 一行；
2. `modelctl logs` / CLI 早退时的 `log_excerpt()` / `tail_file(log, 50)` 摘录同样只能截到容器 ID，进程早退排障形同盲查；
3. loading 段引擎日志模式表（`startup_progress.PATTERNS`）依赖 banner 行做进度锚点，日志缺失则进度永远推不动。

### 解决方案

启动后挂一个独立的**日志 tee 子进程** `docker logs -f --tail all <container>`，把容器输出续写（append）到 launch log：

- **`--tail all` 而非 `--tail 0`**：`docker run --detach` 秒返回，tee 挂上前有数百毫秒窗口，只跟新行会漏掉 banner 首行，伤及模式表匹配；launch log 此前只有一行容器 ID，全量重放无副作用，重复行由 watcher 的 pct 单调不减兜住。
- **append（`"ab"`）+ 独立 PID 文件** `<name>.log-tee.pid`：docker 路径不写引擎 PID（`write_pid=not is_docker`），tee 生命周期必须与引擎 PID 解耦。
- **start 前先清残留 tee**（`start_profile` 开头 `kill_log_tee`）：上一次 start 未走完 / 容器被外力杀时 tee 可能仍在跟随旧容器，双 tee 会重复写同一 launch log。
- **stop / 就绪失败时 kill**：容器被删后 `docker logs -f` 会自行退出，但主动 kill 保证 PID 文件与句柄即时释放。

### 代码示例

```python
# ✅ src/modelctl/core/process.py
def spawn_log_tee(name: str, command: list[str]) -> int | None:
    tp = tee_pid_file(name)          # cache/<name>.log-tee.pid，与引擎 PID 分离
    if tp.is_file():                 # 先清残留 tee，防双写同一 launch log
        ...kill_pid_tree(old); tp.unlink(missing_ok=True)
    path = launch_log(name) or (log_dir() / f"launch-{name}.log")
    fp = open(path, "ab")            # append：保留 start_detached 写的容器 ID 首行
    proc = subprocess.Popen(command, stdout=fp, stderr=subprocess.STDOUT,
                            stdin=subprocess.DEVNULL, start_new_session=True)
    tp.write_text(str(proc.pid), encoding="utf-8")

# ✅ src/modelctl/engines/vllm.py（tokenspeed / tensorrt_llm 同款）
def log_tee_cmd(self) -> list[str] | None:
    if self._resolve_runtime()[0] != "docker":
        return None
    return ["docker", "logs", "-f", "--tail", "all", self._container_name]

# ✅ src/modelctl/core/all_service.py（start_profile 内）
kill_log_tee(profile.name)                     # start 前清残留
...
pid, proc = start_detached(profile.name, cmd, env, write_pid=not is_docker)
if is_docker:
    tee_cmd = adapter.log_tee_cmd()
    if tee_cmd:
        spawn_log_tee(profile.name, tee_cmd)   # launch 段随后挂 tee
...
kill_log_tee(profile.name)                     # stop_profile / 未就绪收尾时 kill
```

```python
# ❌ 反例：--tail 0 漏掉 tee 挂上前数百毫秒的 banner 行，loading 模式表永不命中
return ["docker", "logs", "-f", "--tail", "0", self._container_name]
```

- **关联 commit**：`b27c52d`（start_profile 5 段插桩 + 日志 tee 生命周期）、`379ad13`（适配器 `log_tee_cmd` 钩子）。

## 长启动静默导致误判"卡死"并人为重试杀掉慢而正常的冷启动（Exit 137）

- **日期**：2026-09-08　**分类**：后端 / 引擎启动

### 根因

docker 冷启动（pull 大镜像 + WSL2 内首次加载权重）可达 15 分钟，而旧实现在整个窗口内**零输出**，三个断点叠加：

1. `docker_setup.ensure_image()` 旧版用 `subprocess.run(["docker", "pull", image], capture_output=True)`——pull 的逐层进度全被吞进内存，只在整个 pull 结束后才可能出现在错误文案里，拉一个 20GB 镜像期间 CLI/WebUI 完全静默；
2. `start_profile`（旧 `_do_start`）从拉镜像到健康检查全程不发任何阶段事件，WebUI 只能干转圈；
3. 健康检查超时缺省 600s 对 docker 冷启动不够用，用户/编排层误判"卡死"后重试或强杀，`docker rm -f` 给本在正常加载的容器记 Exit 137——**慢而正常的启动被人为杀死**。

### 解决方案

- **5 段阶段机**：`STAGES = ("preflight", "prepare_env", "launch", "loading", "health")`（`core/startup_progress.py`）。`StartupTracker` 在每个阶段 `begin/progress/done/fail`，双通道输出：`on_progress` 回调（CLI 打单行日志、WebUI 经 `call_soon_threadsafe` 桥到任务 SSE 的 `stage` 帧）+ 快照文件 `cache/<name>.startup.json`（页面刷新/非发起者浏览器轮询 `GET /admin/api/models/{name}/startup` 兜底）。
- **prepare_env 段接 PullParser**：`ensure_image()` 改 `Popen` 流式逐行读（stderr 并入 stdout），每行喂 `PullParser.feed()` 聚合为 [0,1] 总进度回调 `on_progress(label, pct)`；重试前 `parser.reset()`（docker 复用已下载 layer，pct 从当前已完成数重算属真实语义）。
- **loading 段接 LoadingWatcher**：daemon 线程 tail launch log（窗口 400 行），按引擎 `PATTERNS` 模式表（vLLM banner / `Downloading shards` / `Loading safetensors checkpoint shards` / `Capturing CUDA graph shapes` 等）推进子进度；**静默（无新增进展，含日志尚未生成窗口）超 120s → 发兜底文案**「引擎初始化中（首次冷启动在 docker/WSL2 上可达 15 分钟）」，`pct=None` 触发前端条纹动画。
- **超时自适应**：`default_start_timeout()`——`MODELCTL_START_TIMEOUT` 环境变量优先，docker runtime 用 `START_TIMEOUT_DOCKER = 1800`，其余 `START_TIMEOUT_DEFAULT = 600`。
- 阶段耗时按 `engine:stage` 落 `cache/startup-timing.json`（EMA），给出各阶段 ETA。

### 代码示例

```python
# ✅ src/modelctl/core/docker_setup.py：流式 pull + 进度回调（替代 capture_output=True 一把吞）
proc = subprocess.Popen(["docker", "pull", image],
                        stdout=subprocess.PIPE, stderr=subprocess.STDOUT,
                        text=True, encoding="utf-8", errors="replace")
try:
    for line in proc:                       # 逐行：既收集全文供失败分类，也喂解析器
        err_lines.append(line)
        upd = parser.feed(line)             # PullParser：逐层聚合 pct，历史最大保证单调不减
        if upd and on_progress:
            on_progress(upd.label, upd.pct)
except BaseException:
    if proc.returncode is None:
        proc.kill()                         # 中断路径补 kill，防 docker CLI 沦为孤儿
    raise
```

```python
# ✅ src/modelctl/core/all_service.py：5 段插桩 + docker 超时自适应
tracker.begin("prepare_env")
try:
    adapter.pre_start()                     # 内部 ensure_image → on_progress → tracker.progress
except RequirementError as exc:
    tracker.fail("prepare_env", STAGE_LABELS_PREPARE_ENV, str(exc))
    raise
tracker.done("prepare_env")

tracker.begin("loading", "等待引擎初始化")
watcher = LoadingWatcher(tracker, profile.engine, log)  # tail 模式表 + 120s 兜底文案
watcher.start()
try:
    ready = adapter.wait_ready(timeout)     # timeout 缺省：docker 1800s / 其它 600s
finally:
    watcher.stop()
```

```python
# ❌ 反例：pull 输出全被吞，20GB 镜像拉取期间零输出；600s 一刀切误杀 docker 冷启动
subprocess.run(["docker", "pull", image], capture_output=True, timeout=...)
START_TIMEOUT = 600.0
```

- **关联 commit**：`bcd70bc`（StageEvent + StartupTracker）、`4dda6e8`（ensure_image 流式 pull）、`2424297`/`636ceef`（LoadingWatcher 与兜底语义）、`b27c52d`/`3fc6663`/`3df8ae0`（start_profile/cli/webui 超时自适应）。

## docker runtime 下 WebUI `<pre>` 只显示容器 ID + 「vllm」镜像名，fallback 容器日志

- **日期**：2026-09-09　**分类**：后端 / 前端 / 日志

### 根因

`docker run --detach` 客户端秒返回，`launch-<name>.log` 只有一行**容器 ID**（64 位十六进制）+ 镜像名（如 `vllm/vllm-openai:latest` 或 `vllm/vllm-openai:v0.7.0`），容器内真实引擎日志（banner / `INFO: vLLM starting` / ERROR 栈）全部留在 daemon 侧 → `<id>-json.log` / `docker logs`。

即使已有 `spawn_log_tee`（`docker logs -f --tail all` 续写到 launch log），仍有以下断点使 WebUI `<pre>` 退化为容器 ID 单行：

1. **原 SSE 流**（`admin_models.py:_sse_log_stream`）按 `launch-<name>.log` 的字节 offset 推增量——容器刚拉起时 tee 子进程还没追上、或容器还没产出 stdout，launch log 一行不动 → 前端拿到的就是 `<pre>` 渲染出来的固定两行（容器 ID + `vllm`）；
2. **GET /log** 同样只读 launch log 尾部——首屏预填充（`refreshLog`）与"拉取最新"按钮都基于此 → 切详情页同框断点。

### 解决方案

在 `admin_models.py` 新增 4 个 helper + 改 SSE / GET 端点，**docker runtime 自动 fallback 容器日志**：

1. **`_launch_log_effective(lines)`**：复用 `all_service._CONTAINER_ID_LINE`，判定 launch log 是否含有效内容（≥1 行非容器 ID 短词且非 `vllm` 短词）；
2. **`_read_docker_logs(name, tail)`**：`docker logs --tail <N> <container>` 按需拉，异常静默；
3. **`_tail_from_launch_log(name, lines)`**：统一 launch log 尾部读取（避免端点重复写）；
4. **`docker_core_log_path(name)`**：`docker inspect --format {{.LogPath}}` 取容器内 `<id>-json.log` 绝对路径（dockerd 持久写的 json 行日志）；
5. **`_docker_json_line_text(entry)`**：单行 JSON 提取 `log` 字段（docker 一行 = 一次 write syscall，`log` 以 `\n` 结尾，strip 后即 `<pre>` 行文本；解析失败整行透传）。

**端点行为**：

- `GET /admin/api/models/{name}/log`：先读 launch log，**有效直接返回**；无效（仅容器 ID + vllm）→ `docker logs --tail <lines>`，仍空则返回 launch launch 原行；
- `GET /admin/api/models/{name}/log/stream`（SSE）：launch log 无效时，首连接 dump 一次 `docker logs --tail 200` + 记 `<id>-json.log` 当前 `st_size`，**后续每 2s 按字节 offset 推 json.log 增量**（容器存活时新行即推）。文件消失（容器删 / daemon 重启）→ 发 `event: stopped` 帧 + `return`，前端置 closed；
- **前端 `sse.ts`** 注册 `stopped` 命名事件，data 解析 `reason` 字段；`SseLogViewer` 在 `onStopped` 回调里追加「（日志流已停止：<reason>）」行并置 `state=closed`；
- **前端 `ModelDetailView.vue`** 5s 轮询在 `tab==='log'` 时同步调 `refreshLog()`，对 `docker-in-docker` / 容器删后 SSE `stopped` 兜底。

### 代码示例

```python
# ✅ src/modelctl/core/webui/admin_models.py
launch = await asyncio.to_thread(_tail_from_launch_log, name, lines)
if _launch_log_effective(launch):                       # 容器 ID 单行不算有效
    return {"lines": launch[:lines]}
docker = await asyncio.to_thread(_read_docker_logs, name, lines)   # fallback docker logs --tail
return {"lines": docker or launch}[:lines]

# SSE 流：launch log 无效时切换 docker json.log 增量
use_docker_fallback = not _launch_log_effective(initial)
docker_lines = await asyncio.to_thread(_read_docker_logs, name, 200)
json_log_path = await asyncio.to_thread(docker_core_log_path, name)
json_log_pos = (await asyncio.to_thread(json_log_path.stat)).st_size

# 每 2s 轮询
cur = (await asyncio.to_thread(json_log_path.stat)).st_size
if cur > json_log_pos:
    raw = await asyncio.to_thread(_read_file_range, json_log_path, json_log_pos, cur)
    for entry in raw.split("\n"):
        line = _docker_json_line_text(entry)            # docker json.log 行 JSON 解析
        if line:
            yield f"event: log\ndata: {json.dumps({'line': line}, ensure_ascii=False)}\n\n"
    json_log_pos = cur
```

```json
// ✅ docker <id>-json.log 每行格式（一个 write syscall 一行，log 以 \n 结尾）
{"log": "INFO:     Started server process [1]\n", "stream": "stdout", "time": "2026-01-01T00:00:00.000000000Z"}
{"log": "Traceback (most recent call last):\n", "stream": "stderr", "time": "..."}
```

```typescript
// ✅ web/src/api/sse.ts：stopped 命名事件（容器删 / json.log 消失）触发 onStopped
const onStopped: EventListener = (e) => {
  const raw = (e as MessageEvent).data as string | undefined;
  let reason = '';
  if (raw) { try { reason = (JSON.parse(raw) as { reason?: string }).reason ?? ''; } catch { reason = raw; } }
  hooks.onStopped?.(reason);
};
```

```vue
<!-- ✅ web/src/views/ModelDetailView.vue：5s 轮询在 log tab 同步 refreshLog -->
timer = window.setInterval(() => {
  void refresh();
  if (tab.value === 'log') void refreshLog();     // 后端 GET /log 已 fallback docker logs
}, 5000);
```

```python
# ❌ 反例：SSE 仅轮询 launch log，docker runtime 容器 ID 单行后永不更新
while True:
    cur = (await asyncio.to_thread(log_path.stat)).st_size
    if cur > pos:
        for line in _read_file_range(log_path, pos, cur).split("\n"):
            yield f"event: log\ndata: {json.dumps({'line': line}, ensure_ascii=False)}\n\n"
        pos = cur
```

- **关联 commit**：（待记录）。

## EventSource 无首行日志时前端永显「连接中…」（open 事件 + 挂载时机双断点）

- **日期**：2026-09-08　**分类**：前端 / SSE

### 根因

`SseLogViewer.vue` 的状态机是 `connecting → open → closed`，但 `state` **只在 `onLine` 回调里翻转为 `open`**。docker 路径启动初期 launch log 尚无新行（或流刚建立、后端 2s 轮询还没轮到新字节）时，一条 `log` 帧都收不到，状态灯永远显示「连接中…」——实际流早已连通。两个断点：

1. `api/sse.ts` 从未注册 EventSource 的 `open` 事件，传输层"已连接"信号被丢弃（`open` 在首个响应到达即触发，与有无数据帧无关）；
2. 组件在 `v-if` 下挂载时 `props.url` 恒定不变，`watch(url)` 永不触发，`open()` 根本没有调用点——建流时机也缺失。

### 解决方案

- `openModelLogStream()` 增加 `onOpen` 钩子并 `addEventListener('open', ...)`（close 时对称 `removeEventListener`）；后端命名事件 `log` / `heartbeat` 也各自 `addEventListener` 注册（`onmessage` 只派发无名帧）。
- `SseLogViewer.vue` 状态翻转改由 `onOpen` 驱动：`if (state.value === 'connecting') state.value = 'open'`；同时 `onMounted(open)` 保证挂载即建流。

### 代码示例

```typescript
// ✅ web/src/api/sse.ts：注册 open 事件，连接状态由传输层信号驱动
export interface LogStreamHooks {
  onOpen?: () => void;   // 状态由它驱动而非首行日志
  onLine?: (evt: LogSseEvent) => void;
}
const onOpen: EventListener = () => hooks.onOpen?.();
const listeners: Array<[string, EventListener]> = [
  ['open', onOpen], ['log', onLog], ['heartbeat', onHeartbeat], ['error', onErr],
];
for (const [name, fn] of listeners) es.addEventListener(name, fn);
```

```typescript
// ✅ web/src/components/common/SseLogViewer.vue
onOpen: () => {
  if (state.value === 'connecting') state.value = 'open';
},
...
/** 挂载即建流：组件常在 v-if 下挂载，url 恒定不会触发下方 watch，缺此则永停 connecting */
onMounted(open);
```

```typescript
// ❌ 反例：state 只在收到首行日志后才翻转为 open —— 无日志即永远「连接中…」
onLine: (evt) => { state.value = 'open'; push(evt); },
```

- **关联 commit**：`872d145`（SSE onopen 修假连接中）、`5989e41`（SseLogViewer 挂载即建流）。

## docker logs -f 一次性 tee 断流 → loading 进度永远卡死，需保活 supervisor 重连

- **日期**：2026-09-09　**分类**：后端 / 引擎启动 / 日志

### 根因

`spawn_log_tee` 挂载的 `docker logs -f --tail all <container>` 是**一次性子进程**：只要它因任何原因退出（docker CLI 崩溃、daemon 短暂不可达、WSL2 网络抖动、被外力杀），容器输出就**永远不再回写** launch log。连锁失效面（以 qwen2.5-0.5b-vllm 实测为证）：

1. LoadingWatcher tail 的是 launch log，tee 断流后匹配不到任何 vLLM 进度行（banner / 下载权重 / 加载权重 / CUDA graph），**进度永远卡在「加载模型」**，只剩 120s 兜底文案 + 前端条纹动画；
2. WebUI 日志 `<pre>` / SSE / CLI 早退摘录同样只拿到断流前的行——排查时看不到容器真实输出；
3. `modelctl.webui` 的 docker logs fallback（`admin_models`）虽有兜底，但 loading 段的进度上报走的是 launch log，二者脱节。

**实测**（qwen2.5-0.5b-vllm）：tee 进程（PID 20376）已死亡，`docker ps CreatedAt 16:35:16`，容器在 16:46:01 才输出首条 vLLM banner —— 这 10 分 45 秒的**静默期**内 tee 早已断流，容器实际输出全部滞留在 daemon 侧，launch log 只有一行容器 ID，进度上报与真实引擎状态完全脱节。

### 解决方案

把 `spawn_log_tee` 从「直接 Popen `docker logs -f`」改为「spawn 一个**保活 supervisor** 子进程」（`python -m modelctl.core.process --log-tee <container> <launch-log>`），由 supervisor 循环续写并**自动重连**：

- **首轮 `--tail all`**：重放容器已有历史（含 tee 挂上前数百毫秒的 banner，保住 loading 模式表锚点）；
- **重连轮 `--tail 0`**：docker logs 异常退出且**容器仍存活**时，以 `--tail 0` 只跟新行续写——避免全量重放使 launch log 无限膨胀、并挤占 LoadingWatcher 的 tail 窗口漏掉后续真实进度行；
- **退出条件**：`docker_container_alive` 为 False（容器已删/已退出，引擎结束无需续写），或被 `kill_log_tee` / `kill_pid_tree` 终止（stop / 换容器场景）；
- **退避**：断裂后 3s 起步、指数退避、上限 30s；每次重连往 launch log 写一行 `[docker-log-tee]` 痕迹便于排查。
- `.log-tee.pid` 仍指向 supervisor 进程，`kill_log_tee` 语义不变（杀进程树即连 docker logs 子进程一并终止），跨进程 stop 依旧成立。

### 代码示例

```python
# ✅ src/modelctl/core/process.py —— spawn supervisor（替代一次性 docker logs -f）
def spawn_log_tee(name: str, command: list[str]) -> int | None:
    ...  # 清残留 tee
    container = command[-1]                     # ["docker","logs","-f","--tail","all",<container>]
    cmd, env = _log_tee_supervisor_cmd(container, path)
    proc = subprocess.Popen(cmd, env=env, stdout=subprocess.DEVNULL,
                            stderr=subprocess.DEVNULL, stdin=subprocess.DEVNULL,
                            start_new_session=True)
    tp.write_text(str(proc.pid), encoding="utf-8")

def _log_tee_supervisor(container, log_path, retry_sec=3.0) -> None:
    tail = "all"
    while docker_container_alive(container):
        with open(log_path, "ab") as fp:
            p = subprocess.Popen(["docker", "logs", "-f", "--tail", tail, container],
                                 stdout=fp, stderr=subprocess.STDOUT,
                                 stdin=subprocess.DEVNULL, start_new_session=True)
            p.wait()
        if not docker_container_alive(container):
            return
        _write_tee_reconnect(log_path, ...)     # 一行 [docker-log-tee] 痕迹
        time.sleep(retry_sec)
        tail = "0"                              # 只跟新行，不全量重放
```

```python
# ❌ 反例：一次性 docker logs -f —— 子进程任一原因退出即永久断流，进度/日志/摘录全废
proc = subprocess.Popen(["docker", "logs", "-f", "--tail", "all", container],
                        stdout=fp, stderr=subprocess.STDOUT, stdin=subprocess.DEVNULL)
return proc.pid   # 之后无任何重连兜底
```

- **关联 commit**：`b27c52d`（start_profile 5 段插桩 + 日志 tee 生命周期）、`379ad13`（适配器 `log_tee_cmd` 钩子）。（本次保活 supervisor 重连改动待记录）。

## WSL2 上 vLLM 容器 `RuntimeError: UVA is not available` → 需自动注入 VLLM_WSL2_ENABLE_PIN_MEMORY=1

- **日期**：2026-09-09　**分类**：后端 / 引擎启动 / 容器环境

### 根因

WSL2（Docker Desktop WSL2 backend）上 `vllm/vllm-openai:latest` 的 v1 引擎**必崩**，与模型大小、显存无关。vLLM 在 WSL2 下默认 `VLLM_WSL2_ENABLE_PIN_MEMORY=0`（见 `vllm/platforms/cuda.py::is_pin_memory_available`：WSL2 即便内核兼容 pinned memory 也默认关闭，需显式开），于是 `is_uva_available()` 返回 False。而 v1 引擎 `RequestState.__init__` 用 UVA 张量存 `all_token_ids`（`uva_instead_of_gpu=True` 省显存），`UvaBuffer.__init__` 直接：

```python
if not is_uva_available():
    raise RuntimeError("UVA is not available")
```

连锁：EngineCore 子进程启动即抛 → `wait_for_engine_startup` 报 `Engine core initialization failed. Failed core proc(s): {}` → APIServer `sys.exit` → 容器 `Exited (1)`。

**实测时间线（qwen2.5-0.5b-vllm，WSL2 内核 6.18.33）**：08:35 容器创建 → 08:46 banner（静默 10m45s = import vllm+torch+CUDA 上下文）→ 09:31 EngineCore 抛 UVA → exit 1。**每次启动都在同一位置失败**，不是偶发。

> 迷惑点：`Failed core proc(s): {}` 空集合 + 超长静默，极易误判成"显存不足/超时/卡死"。真正致命行是栈底的 `RuntimeError: UVA is not available`，务必翻到 `docker logs` 最底部。

### 解决方案

vLLM docker 分支探测到 docker daemon 跑在 WSL2 内核时，自动 `-e VLLM_WSL2_ENABLE_PIN_MEMORY=1`：

- **判定口径**与 vLLM 自身 `in_wsl()` 一致：`docker info --format {{.KernelVersion}}` 含 `microsoft`（典型 `6.18.33.2-microsoft-standard-WSL2`）。探测封装为 `docker_setup.daemon_is_wsl2()`。
- **注入位置**在 yaml `docker_env` 之前：docker 对重复 `-e` 键**后者覆盖**（实测 `-e FOO=first -e FOO=second` 得 `second`），故用户仍可在 yaml 显式写 `VLLM_WSL2_ENABLE_PIN_MEMORY: "0"` 覆盖。
- **非 WSL2（Linux 裸机部署机）不注入**，避免污染。
- 走 `-e`（进容器），不走 build_command 返回的 env（那只进 docker CLI 宿主进程）。

### 代码示例

```python
# ✅ src/modelctl/core/docker_setup.py
def daemon_is_wsl2() -> bool:
    try:
        out = subprocess.run(["docker", "info", "--format", "{{.KernelVersion}}"],
                             capture_output=True, text=True, timeout=15)
    except (OSError, subprocess.SubprocessError):
        return False
    return out.returncode == 0 and "microsoft" in (out.stdout or "").lower()

# ✅ src/modelctl/engines/vllm.py（build_command docker 分支，TZ 之后、yaml docker_env 之前）
def _wsl2_pin_memory_args(self) -> list[str]:
    return ["-e", "VLLM_WSL2_ENABLE_PIN_MEMORY=1"] if docker_setup.daemon_is_wsl2() else []
```

```python
# ❌ 反例：只改单个模型 yaml 的 docker_env——换模型/换机器即复发，WSL2 是运行时属性而非模型属性
```

- **验证**：`docker run --rm --gpus all -e VLLM_WSL2_ENABLE_PIN_MEMORY=1 --entrypoint python3 vllm/vllm-openai:latest -c 'from vllm.utils.platform_utils import is_uva_available; print(is_uva_available())'` → True。

### 附：为什么"成功启动一次后就快"不成立

`enforce_eager=True` 让 torch.compile / CUDAGraph 那部分缓存本就不产生；剩下的慢是 WSL2 GPU 直通 + GTX 1660 Ti（6GB、无 bf16 回退 fp16）算力，属硬件/架构限制，缓存改变不了。另一半原因（容器可写层当缓存、`rm -f` 每次销毁）见下一节缓存卷。

- **关联 commit**：（待记录）。

## 引擎编译缓存被 `docker rm -f` 每次销毁 → 需 named volume（Windows 上严禁 bind mount）

- **日期**：2026-09-09　**分类**：后端 / 引擎启动 / 容器存储

### 根因

vLLM 把 torch.compile 产物（`VLLM_CACHE_ROOT/torch_compile_cache`）、HF assets、Triton 内核编译缓存（`~/.triton/cache`）全写在**容器可写层**。本工具 `stop` 走 `docker rm -f`，可写层随之销毁，下次启动**全量重编译**（Triton/CUDA 内核可达分钟级）——即"启动过也没变快"的可避免那一半。

### 解决方案

给 vLLM docker 分支挂 **named volume**（`modelctl-<容器名>-cache` → `/root/.cache/vllm`，`modelctl-<容器名>-triton` → `/root/.triton`），跨容器生命周期持久化。三个要点：

1. **Windows 宿主严禁 bind mount**：Windows 路径 bind 进 Linux 容器走 9p/virtiofs，编译缓存这类小文件随机读写会慢到**可能反超"不挂缓存"**。named volume 落在 Docker Desktop 的 WSL2 ext4（实测 `findmnt` → `/dev/sdf[/data/docker/volumes/...]`），IO 接近原生。
2. **直接挂默认目录，别用 env 重定向**：实测 `VLLM_ASSETS_CACHE` **不随** `VLLM_CACHE_ROOT` 联动（设了 `VLLM_CACHE_ROOT` 后 assets 仍写 `/root/.cache/vllm/assets`），靠 env 改路径会静默漏掉 assets。挂父目录 `/root/.cache/vllm` 天然覆盖，且少一环出错。（镜像以 root 运行、`HOME=/root`，故路径写死可接受。）
3. **volume 名必须 sanitize**：Docker 只允许 `[a-zA-Z0-9][a-zA-Z0-9_.-]*`，profile.name 含空格/中文/非法前缀会让 `docker run` 直接 "volume name is invalid"，**容器根本起不来**。用 `_cache_volume_name()` 清洗并兜底 `default`。

> `docker rm` 容器**不会**带走卷（这是持久化的前提）。清缓存需显式：
> `docker volume rm modelctl-<容器名>-cache modelctl-<容器名>-triton`

### 代码示例

```python
# ✅ src/modelctl/engines/vllm.py
_ENGINE_CACHE_DIRS = {"cache": "/root/.cache/vllm", "triton": "/root/.triton"}
_VOL_NAME_BAD = re.compile(r"[^a-zA-Z0-9_.-]")

def _cache_volume_name(profile_name: str) -> str:
    return _VOL_NAME_BAD.sub("-", (profile_name or "").strip()).strip("-.") or "default"

# build_command docker 分支：与 env_args 同段插在 --ipc=host 之前（选项须先于镜像名）
insert = env_args + self._cache_volume_args()   # -v modelctl-<ctr>-cache:/root/.cache/vllm ...
cmd[cmd.index("--ipc=host"):cmd.index("--ipc=host")] = insert
```

```python
# ❌ 反例 1：bind mount 工程目录——9p/virtiofs 让小文件随机 IO 比不挂缓存还慢
args += ["-v", f"{PROJECT_ROOT / 'data' / 'engine-cache'}:/root/.cache/vllm"]
# ❌ 反例 2：只设 env 不改挂载点（或只挂 VLLM_CACHE_ROOT 却漏 assets）
["-e", "VLLM_CACHE_ROOT=/mnt/cache"]   # assets 仍写镜像可写层，rm -f 照样丢
```

- **关联 commit**：（待记录）。

## loading 兜底文案一次性 → 用户对着静止条纹干等 30 分钟，需分级递进

- **日期**：2026-09-09　**分类**：后端 / 引擎启动 / 可观测性

### 根因

`LoadingWatcher` 原本只发**一次**兜底文案（`_fallback_sent` 布尔门），文案固定「引擎初始化中（首次冷启动可达 15 分钟）」。当启动真的异常缓慢或卡死（如上面的 UVA 崩溃前、或显存不足 OOM 边缘）时，用户在一级文案触发后到 docker 1800s 超时前，看到的是**同一句话 + 静止条纹动画长达近半小时**，无法区分"还在正常预热"与"已经卡死该去看日志了"。

### 解决方案

把单次兜底改为**三级递进**（`_LOADING_FALLBACK_STEPS`，按一级阈值的倍率 1×/5×/10× = 120s/600s/1200s）。静默时长跨到更高一级即发更深提示，同级不重发；出现新进展后重置级别，可再次逐级递进。最高一级（10×=1200s）在 docker 1800s 超时前给出"疑似卡死、请查日志"终态提示。

### 代码示例

```python
# ✅ src/modelctl/core/startup_progress.py
_LOADING_FALLBACK_STEPS = (
    (1.0, "引擎初始化中（首次冷启动在 docker/WSL2 上可达 15 分钟）"),
    (5.0, "仍在初始化（GPU 上下文/内核预热较慢，已超 10 分钟）——请留意容器日志是否报错"),
    (10.0, "初始化异常缓慢（已超 20 分钟）：疑似显存不足或 CUDA 初始化卡死，请查看容器日志排错"),
)
# _tick 内：注入 fallback_sec 时各级 = fallback_sec × 倍率，整体缩放便于单测
self._steps = [(mult * self._fallback_sec, label) for mult, label in _LOADING_FALLBACK_STEPS]
# 进度判定：target = 静默时长跨越的最高级别下标，target > _fallback_level 才发且不重发
```

```python
# ❌ 反例：布尔门一次性——跨越任何时长都只有那一句，无法向用户传达"越来越不对劲"
elif not self._fallback_sent and (now - self._last_advance_ts) > self._fallback_sec:
    self._fallback_sent = True
    self._tr.progress("loading", _LOADING_FALLBACK_LABEL, pct=None)
```

- **关联 commit**：（待记录）。
