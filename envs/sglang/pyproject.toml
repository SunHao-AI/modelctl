[project]
name = "modelctl-venv-sglang"
version = "0.1.0"
description = "SGLang 引擎专属虚拟环境（由 modelctl env setup 管理）"
requires-python = ">=3.12"
dependencies = [
    "sglang[all]>=0.5.9,<0.6",
]

# index 配置继承项目根 uv.toml（刻意不写 [tool.uv]，见根 uv.toml 顶部说明）。
