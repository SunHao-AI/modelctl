[project]
name = "modelctl-venv-vllm"
version = "0.1.0"
description = "vLLM 引擎专属虚拟环境（由 modelctl env setup 管理）"
requires-python = ">=3.12"
dependencies = [
    "vllm>=0.27,<0.28",
]

# index 配置继承项目根 uv.toml（本文件刻意不写 [tool.uv]：uv 一旦发现 [tool.uv]
# 就停止向上查找，会丢失根配置的镜像优先级，详见根 uv.toml 顶部说明）。
