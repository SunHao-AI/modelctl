# ollama/qwen3.8.yaml —— Qwen3.8-27B（ollama 引擎）
# 学习要点：ollama 引擎是常驻 serve 进程，模型按需加载/卸载，无需 download 段（由 ollama pull 处理）。
group: qwen3.8
# port：服务监听端口（ollama serve 常驻端口）
port: 11434
# 注意：ollama 引擎无需 api_key（本地服务，无 OpenAI 兼容鉴权）

ollama:
  # model：ollama 模型名（<name>:<tag>），不存在时 pre_start 自动 `ollama pull`
  # 注意：库名为 qwen3.8（qwen3 库无 27b tag）
  # 默认启用 MTP 投机解码官方推荐 tag `27b-mtp-q4_K_M`（18GB，MTP 头 + Q4_K_M）；
  #   实测 MTP 接受率为负或拉取失败（tag 不存在/带宽不足）可回退 `qwen3.8:27b`（无 MTP 头）
  # bind_host：服务绑定地址（安全加固，2026-09-08，所有引擎统一默认）
  # 默认 127.0.0.1 仅回环监听：外部（含 AI Agent / 其他内网主机）无法直连引擎端口，
  # 从而堵住绕过网关鉴权/账号/多 Key/token 限速/审计的“第二入口”。
  # 确需对外暴露时改为 0.0.0.0，并配合防火墙/白名单限制来源（非默认，风险自担）。
  bind_host: 127.0.0.1
  model: qwen3.8:27b-mtp-q4_K_M
  # keep_alive：模型驻留内存时长（秒）；-1 = 永久驻留，0 = 用完即卸载
  keep_alive: -1
  # num_parallel：并发请求数（OLLAMA_NUM_PARALLEL）
  num_parallel: 1
  # context_length：上下文长度（OLLAMA_CONTEXT_LENGTH）
  # 注意：单请求完整可用（与并发无关）；KV 显存 ≈ 并发 × context_length
  context_length: 65536
  # gpu_list：限制 ollama serve 进程可见的 GPU（可选，默认不设=全部可见）；
  # 注意：ollama 为常驻进程，所有 ollama 模型共享该限制，无法按单模型隔离
  # 示例：gpu_list: "0,1,2,3"
